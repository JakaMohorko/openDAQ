# Review Feedback Backlog (`// COMMENT:` annotations)

Source: reviewer comments added to the `refactor/AI-refactor` branch (commit of 2026-07-17).
Each entry records the comment, the answer/analysis, and the disposition. Items marked
**Done** were addressed immediately; **Backlog(phase)** items are scheduled; **Answered**
items needed a factual answer, not a code change.

Legend: P2b = current phase (MultiReaderImpl swap), P3 = status/state extension,
P5 = resampling, P6 = cleanup/deprecation.

## queue_reader.h / queue_reader.cpp

| #   | Location                                                  | Comment (gist)                                                                                                                                   | Disposition                              | Analysis / answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Feedback                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `queue_reader.h` (SignalEventType)                        | Domains can be set to nullptr — do events account for it?                                                                                        | **Done** (P2b)                           | They did **not**: `SignalEvent` derived its type from `descriptor.assigned()`, so a "descriptor changed to null" event collapsed to `NoChange`, and `toEventPacket()` re-emitted *unchanged* descriptors as the explicit null marker ("removed"). Fixed: `SignalEvent` now stores explicit `valueDescriptorChanged`/`domainDescriptorChanged` flags from `parseDataDescriptorEventPacket`, merge propagates flags (newest descriptor wins, including null), and `toEventPacket()` emits absent-vs-null-marker correctly. `addEncounteredEvent` consequently clears the cached descriptor on a change-to-null, and the missing-descriptor check in `SynchronizationManager::buildCommonModel` drives the reader to `WaitingForDescriptors`. | Ok                                                                                                                                                                                                                                                                                                    |
| 2   | `queue_reader.h` `UnsupportedDimensions`                  | "There should be no unsupported dimensions"                                                                                                      | **Backlog (P2b/P3)** — rename only       | Value descriptors of any rank are readable (valuesPerSample = product of dims). The flag remains only for **domain** descriptors with `dimensionCount != 0` — a vector timestamp has no meaning for synchronization. Proposal: rename to `DomainNotScalar` for clarity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | DomainNotScalar is ok, but in general, there are very strict restrictions for domains. I would simply have a rigorous domain descriptor check.                                                                                                                                                        |
| 3   | `queue_reader.h` `seedDescriptors`                        | Will probably be removed at the end?                                                                                                             | **Backlog (P6)**                         | Yes. It exists solely for the reader-from-existing migration; it goes away in P6 together with `MultiReaderFromExisting` and the `MultiReaderImpl(MultiReaderImpl*, …)` constructor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Ok                                                                                                                                                                                                                                                                                                    |
| 4   | `queue_reader.h` transform functions                      | Still needed?                                                                                                                                    | **Answered** (keep; see #6)              | Yes for API compatibility: `ISampleReader::setValueTransformFunction`/`setDomainTransformFunction` are public. The QueueReader is where the transform is applied to raw data today. Whether domain transforms should move out of QueueReader and apply once at the facade output is noted in #6.                                                                                                                                                                                                                                                                                                                                                                                                                                           | I would add the removal of transform functions as a possible API breaking change that's done at the end of this rework. We should consider deprecating parts of the API if it serves no purpose/is outdated.                                                                                          |
| 5   | `queue_reader.h` `read(valueBuffer, domainBuffer, count)` | (a) Do we need per-signal domain buffers? (b) Divided-rate buffers with non-divided count is inconsistent; divider math is spread around.        | **Backlog (P5)**                         | (a) Per-signal **native** domain output is compat-critical for the direct path (spec issue #6: `readWithDomain` keeps native domain values; existing tests assert per-signal timestamps). For the resampled path (P5) the common grid is computed once — there the per-signal domain buffer is indeed redundant, and the coordinator can synthesize it. (b) Agreed: the common-rate-count / native-buffer split is confusing. Plan: make `ReadCoordinator` the *only* place doing divider math — QueueReader gets a native-count API and the coordinator converts once (P5 refactor window, after direct path is green).                                                                                                                   | We can also keep this for the end, but I think the `readWithDomain`API call should be reworked to only return the main rate domain buffer. All other signals anyhow need to be synced to that rate. The multipliers/dividers, however, need to be exposed on the API (probably in the read statuses). |
| 6   | `queue_reader.h` `TypedReadingContext`                    | Per-signal domain out types/transforms don't fit sync signals; prioritize usability over full API retention; apply domain transforms at the end. | **Backlog (P5)**                         | Agreed in principle. Constraint for P2b: all QueueReaders already share one `resolvedDomainReadType`, so per-signal domain *types* are already unified. Moving domain transform application from QueueReader to the facade output stage is a clean follow-up; flagged as a P5 refactor together with #5 so the buffer plumbing changes once, not twice.                                                                                                                                                                                                                                                                                                                                                                                    | Check the previous response. Domains for synchronous signals should be output only in 1 buffer, not a buffer per signal read. This can be done as a later step, however.                                                                                                                              |
| 7   | `queue_reader.cpp` (SignalEvent, top)                     | Revisit how IMultiReaderStatus reports events; event packets not intuitive.                                                                      | **Backlog (P3)**                         | Spec already plans this: P3 adds the ordered event list (`getEventCount`/`getEvent(index → inputIndex, eventPacket)`) while keeping the `getEventPackets` dict for compat. A fully typed event object (beyond event packets) would be an API break — out of scope for this rework.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Ok. Make it a non-breaking change, but we should plan for a later step to break and update the API to make it more usable.                                                                                                                                                                            |
| 8   | `queue_reader.cpp` ctor                                   | ReadMode vs read types correlation weird; Unscaled ignored — clarify.                                                                            | **Backlog (P2b docs)**                   | Current semantics: `RawValue` forces the value output type to the signal's raw type (post-scaling input); `Scaled` applies post scaling then converts to the requested type; `Unscaled` behaves like `Scaled` minus post scaling — the type selection happens in `parseValueDescriptor` (`typeCtx.valueIn` choice), which is why the ctor only special-cases `RawValue`. Needs a documenting comment + a spec §6 clarification paragraph rather than a behavior change.                                                                                                                                                                                                                                                                    | Yes, clarify the behaviour, but don't hesitate to suggest a change that makes the settings more clear as a further post-rework API breaking change.                                                                                                                                                   |
| 9   | `queue_reader.cpp` `getAvailableSamplesUntilEvent`        | Divided (common-rate) or native — which is clearer?                                                                                              | **Answered** (common-rate; unify per #5) | Keep **common-rate** for owner-facing APIs: `ReadCoordinator` compares availability across inputs and floors to blockLcm — that only works in a single unit. Per #5, native units will live behind the QueueReader boundary only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Ok. The only issue I see is that the buffers need to be in the "native" size, not the common one. Meaning that the user-provided buffers need to be scaled, but the count I suppose is kept in the common one.                                                                                        |
| 10  | `queue_reader.cpp` `hasPendingEvents`                     | checkConnection/drainConnection mid-API-call mutates state; prefer ready-check → update state → execute.                                         | **Backlog (post-P2b)**                   | Valid architectural point. Today every QueueReader accessor lazily drains. Target model: the owner (MultiReaderImpl) drains explicitly at evaluation points (`evaluateStateLocked` step 4/5) and accessors become const queries. This is a contained refactor but touches every call site; scheduled right after P2b is green so behavior diffs are attributable.                                                                                                                                                                                                                                                                                                                                                                          | Ok                                                                                                                                                                                                                                                                                                    |
| 11  | `queue_reader.cpp` `isValid`                              | Still needed? Maybe just a "ready" flag; error states more explicit now.                                                                         | **Backlog (post-P2b, with #10)**         | Mostly agreed: `issues.empty()` is the real signal, and `MultiReaderImpl` consumes issues per-slot for `Incompatible` state anyway. `isValid()` remains as a convenience for unit tests. Fold into #10's accessor cleanup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Ok                                                                                                                                                                                                                                                                                                    |

## domain_value.h

| #   | Location                               | Comment (gist)                                                                           | Analysis / answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Disposition                  | Feedback                                                                                                  |
| --- | -------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------- |
| 12  | `DomainValue::domain` member           | Why is DomainInfo part of the value?                                                     | A `DomainValue` is only meaningful relative to an epoch+resolution; carrying the `DomainInfo` makes values self-describing so `compare`/`toCommonDomain` can be safe without external bookkeeping. The alternative (caller passes both DomainInfos to every operation) spreads the invariant. Keep composition, document the rationale on the member.                                                                                                                                                                        | **Answered / doc comment**   | Ok, but maybe check whether the QueueReader needs a getter for the domain info, or just the domain value. |
| 13  | `toCommonDomain`/`fromCommonDomain`    | Heavy duplication; "common" naming odd (suggest `toDomain`); are both directions needed? | Naming: agreed — they convert to/from an *arbitrary target* domain; rename to `toDomain(const DomainInfo&)` / and drop the "common" framing. Both directions **are** needed by the sync loop: firsts are converted *to* the common domain to pick the latest start, and the target is converted back *from* the common domain into each signal's own domain for `advanceToDomainValue`. Duplication: extract the epoch-offset + multiplier computation (shared by scalar, RangeType64, and both directions) into one helper. | **Backlog (P2b/P3 cleanup)** | Ok                                                                                                        |
| 14  | `DomainValueImpl<RangeType64>`         | Same duplication in the specialization.                                                  | Covered by the #13 helper extraction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Backlog (with #13)**       | Ok                                                                                                        |
| 15  | `DomainValueImpl<ComplexFloat32>` etc. | Should be a compile-time error, not runtime NotSupported.                                | Agreed in principle; the blocker is that `visitTwoSampleTypes` dispatches over *runtime* SampleType, so every instantiation must compile. Fix shape: constrain the visitor for domain reads to integral/float/RangeType64 tags (`if constexpr` short-circuit before instantiating `DomainValueImpl`), then delete the throwing specializations.                                                                                                                                                                              | **Backlog (with #13)**       | Ok                                                                                                        |

## typed_reading_utils

| #   | Location                        | Comment (gist)                                                           | Analysis / answer                                                                                                                                                                                                                                                                                                                                                | Disposition                           | Feedback |
| --- | ------------------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------- |
| 16  | `typed_reading_utils.cpp` (top) | File hard to parse; simplify, and drop cases the multi reader disallows? | Partly inherent (N×M sample-type dispatch), but two real simplifications exist: (a) domain reads only ever target one integral output type (`resolvedDomainReadType`) — the domain dispatch can be one-dimensional; (b) after #15 the non-domain-capable types drop out of domain paths entirely. Value-side conversion matrix must stay (public read-type API). | **Backlog (P5 window)**               | Ok       |
| 17  | `createReadLayout`              | Unused?                                                                  | Unused by production code — only `test_typed_reading.cpp` calls it; `QueueReader::parse*Descriptor` builds layouts inline (it also folds issue detection in). Either delete it and build layouts in the tests manually, or (preferred) have `parse*Descriptor` call it so there is one layout builder.                                                           | **Backlog (small, with #10 cleanup)** | Ok       |
| 18  | `readDomainValue`               | Do we really need to convert domain values?                              | Yes — it is the heart of synchronization: `getFirstSampleDomainValue`/`advanceToDomainValue` materialize a packet's first-sample domain value so the sync manager can compare starts across signals in the common domain and verify the reached tick (spec §5, reached-value verification). Not removable while sync works on domain values.                     | **Answered** (keep)                   | Ok       |

## multi_reader_impl.h

| #   | Location        | Comment (gist)                            | Analysis / answer                                                                        | Disposition      | Feedback |
| --- | --------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------- | -------- |
| 19  | old-reader ctor | "Can likely be removed in future phases." | Confirmed — P6 removes `MultiReaderFromExisting`, this ctor, and `seedDescriptors` (#3). | **Backlog (P6)** | Ok       |

## Plan adjustments made

1. **P2b (now):** SignalEvent change-flag fix (#1) applied immediately — it also corrects the
   event packets surfaced through `IMultiReaderStatus::getEventPackets`.
2. **Post-P2b cleanup step added** (before P3): explicit-drain refactor (#10), `isValid` fold (#11),
   `createReadLayout` unification (#17), `UnsupportedDimensions` → `DomainNotScalar` rename (#2),
   DomainValue rename + dedup + compile-time constraints (#12–#15).
3. **P5 window:** divider-math consolidation into ReadCoordinator + domain-transform relocation
   (#5, #6) executed together with the resampling buffer changes; typed-reading simplification (#16).
4. No change to the agreed phase order.
